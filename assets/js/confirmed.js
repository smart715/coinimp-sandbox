import { FontAwesomeIcon, FontAwesomeLayers } from '@fortawesome/vue-fontawesome'
let content = new Vue({
    el: '#signup-confirmed',
    components: {
        FontAwesomeIcon,
	FontAwesomeLayers 
    }
});
